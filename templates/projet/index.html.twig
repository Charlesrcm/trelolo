{% extends 'base.html.twig' %}

{% block title %}Projet !{% endblock %}

{% block body %}

<div class="container-fluid mt-3 d-flex justify-content-between">
    <h1>Page des projets</h1>
    <div class="align-self-center">
        <a class="btn btn-primary text-decoration-none text-light" href="/admin">Ajouter un projet</a>
        <a class="btn btn-primary text-decoration-none text-light" href="/admin">Ajouter une tâche</a>
    </div>
</div>
<div class="mt-4 d-flex justify-content-between">
    {% for prio in prioriteListe %}
    
        <div class="col-4 border border-dark rounded row justify-content-center">
            <h3 class="text-center">{{prio.libelle}}</h3>
            {% for projet in projetListe %}

            {# test pour mettre les projet dans chaques colonnes #}
            {% if (projet.priorite is same as prio) %}
            <div class="border border-info border-3 rounded my-3">

                <div class="row mt-2">
                    <h4 class="col-5">{{projet|capitalize}}</h4>
                    <p class="col-7 px-0">Administrateur : {{projet.utilisateur}}</p>
                </div>

                {% for tache in tacheListe %}
                {% if (tache.projet is same as projet)%}
                <div id="tache_card" class="card d-flex justify-content-center mb-3">
                    <div class="d-flex justify-content-between align-items-center card-header bg-black bg-gradient bg-opacity-50">
                        <h5 class="text-light">{{ tache|capitalize }}</h5>
                        {# <a class="badge text-decoration-none" href="" style="background: {{ tache.etat.color }}"></a> #}

                        <div class="dropdown">
                            <button class="btn dropdown-toggle text-light" style="background: {{ tache.etat.color }}" type="button" id="dropdownMenuButton1" data-bs-toggle="dropdown" aria-expanded="false">
                                {{ tache.etat }}
                            </button>
                            <ul class="dropdown-menu" aria-labelledby="dropdownMenuButton1">
                                {% for etat in etatListe %}
                                {% if (tache.etat is not same as(etat)) %}
                                    <li><a class="dropdown-item" id="listEtat">{{ etat }}</a></li>
                                {% endif %}
                                {% endfor %}
                            </ul>
                        </div>

                    </div>
                    <div class="card-body">
                        <h6 class="card-title"><strong>Attribué à : </strong>{{ tache.utilisateur }}</h6>
                        <p class="card-text"><strong>Description : </strong>{{ tache.description }}</p>
                    </div>
                </div>
                {% endif %}
                {% endfor %}

            </div>
            {% endif %}
            {% endfor %}
        </div>
    {% endfor %}
</div>
{% endblock %}


