{% extends 'base.html.twig' %}

{% block title %}Projet !{% endblock %}

{% block body %}

<div class="container-fluid mt-3 d-flex justify-content-between">
    <h1>Page des projets</h1>
    <button class="btn btn-primary"><a class="text-decoration-none text-light" href="{{ path('new_projet')}}">Ajouter un projet</a></button>
</div>
<div class="mt-4 d-flex justify-content-between">
    {% for prio in prioriteListe %}
        <div class="border">
            <h3>{{prio.libelle}}</h3>
            <div>
                
            </div>
        </div>
    {% endfor %}
</div>
{% endblock %}
